# This is an example backup config.
# It uses the protocol S3 to backup to Backblaze.

export BACKUP_TYPE=s3
export BACKUP_PATH=/some/path/to/back/up

export BUCKET_NAME=my-backup-bucket
export BUCKET_PREFIX=/my-backup-data #Can be blank for no prefix

export S3_DOMAIN=s3.us-west-002.backblazeb2.com

export AWS_ACCESS_KEY_ID=002257f55b73fd80000000014
export AWS_SECRET_ACCESS_KEY=K002JZGnGoX+vkwDe9Blzf0xvszOTBg
